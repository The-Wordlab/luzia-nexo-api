# Examples

Runnable Nexo integration examples.

Folders:
- `webhook/minimal`
- `webhook/structured`
- `webhook/advanced`
- `partner-api/proactive`

Run all example tests from the repo root:

```bash
make test-examples
```
